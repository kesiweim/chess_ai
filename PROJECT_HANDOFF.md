# Chess AI 项目交接说明

更新时间：2026-09-11

## 1. 项目目标

这是一个只读取棋谱/局面、不做图像识别的国际象棋 AI。当前有两条明确的产品线：

- **Material 1.0.0**：冻结版本，用于基准和回归，不要改动。
- **Neural 2.x.x**：当前主要开发线，重点是神经策略/价值网络、自博弈和残局能力。

## 2. 版本与资产规则

Material 1.0.0 的发布基线包括：

- `chess_model_balanced.pt`
- `syzygy_v4.py`
- `syzygy_mate_candidate.py`
- 运行入口以仓库实际文件为准（常见为 `play_search.py`/Material 入口）。

Neural 线的候选模型包括 v4 原版、self-play round 008、round 015；权重必须使用完整绝对路径，并在报告中记录 SHA-256。不要覆盖原版、r8 或 r15。

大文件（`.pt`、JSONL 数据集、Syzygy 表库、ZIP 报告）不要未经确认提交到 Git；用云端持久目录或单独的资产包保存，并记录路径和哈希。

## 3. 当前开发重点

1. 使用 r15（或经评估后更优的候选）继续 Neural 自博弈。
2. 每轮约 40 局，使用 GPU 训练 2～3 轮；混合旧人类棋谱，避免灾难性遗忘。
3. 云端单次最长 8 小时：总控最多运行约 7.5 小时，并预留至少 30 分钟写报告、保存权重和压缩结果。
4. 每轮必须保存：对局、训练/验证指标、候选权重、配置、代码哈希；中断后从最近完成的轮次恢复。
5. 残局使用 Syzygy（可用时优先；缺表时回退神经搜索），但表库不是训练启动的必要条件。

## 4. 推荐执行顺序

### 阶段 A：只审计，不训练

先确认当前目录、分支、依赖、模型和数据是否存在；运行脚本的 `--help` 和最小导入测试。输出一份清单后等待确认。

### 阶段 B：小规模验证

先只跑 1 局或 1 个极小批次，验证模型能加载、GPU 可用、局面能落子、报告能写出；不要把 `--smoke` 当成正式训练，很多 smoke 模式只做快速检查后就退出。

### 阶段 C：正式循环

使用仓库中已经验证过的 v4 自博弈总控（若存在，优先 `v4_selfplay_loop/run_v4_loop.py`），通过 `--policy` 和 `--value` 指定 r15 权重；只有确认小规模验证通过后，才打开 `--use-candidate` 自动进入下一轮。

建议参数：`games=40`、GPU、每轮训练 2～3 epoch、总时限 7.5 小时、预留 30 分钟。每次先查看 `--help`，不要假设不同脚本支持同名的 `--resume`。

## 5. 已知问题与避坑

- 旧的 `chess_loop_r15/run.py` 曾硬编码不存在的 Syzygy 文件和 r15 权重路径，且云端缺少 `chess` 包；未修复前不要把它当作正式总控。
- 不能用“替换一个文件名”的方式猜路径；先 `Test-Path`（Windows）或 `test -f`（Linux）逐个确认。
- `python-chess`、`numpy`、与当前 PyTorch/CUDA 必须在同一环境中安装；先打印 `torch.cuda.is_available()` 和 GPU 名称。
- 不删除旧运行目录，不覆盖冻结模型；训练结束前不要关闭终端或删除日志。
- 所有结论必须基于实际生成的报告，不能只根据终端最后一行推断“已完成”。

## 6. 评估协议

每个候选至少检查：

- 普通验证集的 loss/准确率；
- 合法走法首选准确率和 Top-3；
- 将死测试（首选和 Top-3）；
- 固定残局测试与 Syzygy 一致率；
- 与冻结的 Material 1.0.0、v4 原版的固定局数对战。

只有在没有明显遗忘、残局回归通过、对战不低于基线时，才把候选标为下一轮起点。

## 7. 接手方第一条指令

“你现在接手本项目。先阅读 `PROJECT_HANDOFF.md`，再只做审计：列出目录/分支/依赖/模型/数据/脚本、检查每个路径和导入、运行最小自测，并报告所有阻塞项。此阶段禁止训练、删除、覆盖或上传任何文件；报告完成后等待确认。”

